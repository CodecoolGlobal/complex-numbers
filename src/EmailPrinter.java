import java.util.List;

public class EmailPrinter implements Printer {

    // EmailSender sender = new EmailSender();

    @Override
    public void printList(List<ComplexNumber> listOfNumbers) {
        // sender.send(recipient, listOfNumbers);
    }
}
